# No server API keys are required.
# Users bring their own keys in the browser (BYOK). LLM costs are billed to them.

# Optional server flags (self-host / Vercel):
# DISABLE_PYTHON=1          # Disable server Python tool (auto-disabled on Vercel)
# NODE_ENV=production
