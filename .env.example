# No server API keys are required.
# Users bring their own keys in the browser (BYOK). LLM costs are billed to them.

# Optional server flags:
# ALLOW_PRIVATE_PROVIDER_URLS=true        # Self-hosted only: permit localhost/private custom providers, SearXNG, and remote MCP URLs in production
# NODE_ENV=production
